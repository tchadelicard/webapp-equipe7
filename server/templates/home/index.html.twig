{#
Binôme 21
Yiré Soro, Tchadel Icard
#}

{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur mes wishlists{% endblock %}

{% block body %}
    <style>
        /* Ajustement de la marge supérieure pour la section héros afin de tenir compte de la barre de navigation fixe */
        .hero-section {
            background: url('{{ asset('images/background.png') }}') center center/cover no-repeat;
            color: #fff;
            padding: 200px 0 120px;
            text-align: center;
            position: relative;
            width: 100%;
        }
        .hero-section .overlay {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: rgba(0, 0, 0, 0.4);
        }
        .hero-content {
            position: relative;
            z-index: 2;
        }

        /* Styles pour la section des fonctionnalités */
        .features .feature-icon {
            font-size: 3rem;
            color: #00B8DE;
            margin-bottom: 1rem;
        }
    </style>

    {# Section Héros #}
    <section class="hero-section">
        <div class="overlay"></div>
        <div class="hero-content">
            <h1 class="display-4 fw-bold">Partagez vos envies avec le monde !</h1>
            <p class="lead">Créez, éditez et partagez vos listes de souhaits en toute simplicité</p>
            <p>
                <a href="{{ path('app_wishlist_list') }}" class="btn btn-primary btn-lg" style="background-color: #00B8DE;">Commencer maintenant</a>
            </p>
        </div>
    </section>

    {# Conteneur principal pour le reste du contenu #}
    <div class="container text-center py-5">
        {# Affichage des messages de succès #}
        {% for flash_success in app.flashes('success') %}
            <div class="alert alert-success text-center alert-dismissible fade show" role="alert">
                {{ flash_success }}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fermer"></button>
            </div>
        {% endfor %}

        {# Affichage des messages d'avertissement #}
        {% for flash_warning in app.flashes('warning') %}
            <div class="alert alert-warning text-center alert-dismissible fade show" role="alert">
                {{ flash_warning }}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fermer"></button>
            </div>
        {% endfor %}

        {% if app.user and not app.user.isVerified %}
            <div class="alert alert-warning text-center">
                Votre adresse e-mail n'est pas vérifiée ! Veuillez consulter votre boîte de réception pour confirmer votre e-mail.
                <br><a href="{{ path('app_verify_email_resend') }}" class="btn btn-primary btn-sm mt-2">Renvoyer l'e-mail de vérification</a>
            </div>
        {% endif %}

        {# Section des fonctionnalités #}
        <section class="features py-5">
            <div class="container">
                <div class="text-center mb-5">
                    <h2 style="color:#99cc99;">Réalisez vos rêves... et ceux de vos proches !</h2>
                    <p class="text-muted">Grâce à notre plateforme de listes de souhaits, facilitez vos partages et surprenez vos amis.</p>
                </div>
                <div class="row">
                    <div class="col-md-4 text-center mb-4">
                        <i class="fas fa-list feature-icon"></i>
                        <h4 class="fw-bold">Créez et éditez vos listes de souhaits</h4>
                        <p class="text-muted">
                            Notez vos envies et mettez-les à jour facilement pour que tout le monde sache ce qui vous fait plaisir.
                        </p>
                    </div>
                    <div class="col-md-4 text-center mb-4">
                        <i class="fas fa-share feature-icon"></i>
                        <h4 class="fw-bold">Partagez-les avec vos proches ou rejoignez leur liste</h4>
                        <p class="text-muted">
                            Envoyez votre liste à vos amis ou consultez la leur pour leur faire plaisir tout en évitant les doublons.
                        </p>
                    </div>
                    <div class="col-md-4 text-center mb-4">
                        <i class="fas fa-gift feature-icon"></i>
                        <h4 class="fw-bold">Réalisez leurs souhaits</h4>
                        <p class="text-muted">
                            Découvrez les rêves de vos proches et offrez-leur des cadeaux qui font vraiment plaisir !
                        </p>
                    </div>
                </div>
            </div>
        </section>
    </div>
{% endblock %}
